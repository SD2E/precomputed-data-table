name: fcs_signal_prediction
channels:
- defaults
dependencies:
    - python = 3.7
    - pip

    - pip:
      - flowcytometrytools
      - numpy
      - pandas
      - scikit-learn
#      - /src/fcs_signal_prediction