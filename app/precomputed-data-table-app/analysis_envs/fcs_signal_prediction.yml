name: fcs_signal_prediction
channels:
- defaults
dependencies:
    - python = 3.7
    - pip
    - matplotlib = 3.1

    - pip:
      - flowcytometrytools
      - numpy
      - pandas
      - scikit-learn
      - matplotlib
#      - fcs_signal_prediction
#      - pysd2cat
      - /src/pysd2cat
      - /src/fcs_signal_prediction